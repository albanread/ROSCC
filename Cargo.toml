[package]
name = "roscc"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
llvm-sys = { version = "221.1.0", optional = true }

[features]
default = []
# `ingest`/`demo` need a linked LLVM (llvm-sys); the linker does not.
llvm = ["dep:llvm-sys"]
